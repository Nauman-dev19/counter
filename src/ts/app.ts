// const counter = document.querySelector(`.counter`);
// const counterValueArea = document.querySelector(`.count-value`);
// const counterBtns = document.querySelector(`.count-btns`);

import { btns } from './btns.js';
btns();
import { counter } from './counter.js';
counter();
